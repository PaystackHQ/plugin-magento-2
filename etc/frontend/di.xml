<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <!-- Payment verify API (frontend + webapi_rest only — never in admin) -->
    <preference for="Pstk\Paystack\Api\PaymentManagementInterface" type="Pstk\Paystack\Model\PaymentManagement"/>
    <type name="Pstk\Paystack\Model\PaymentManagement">
        <arguments>
            <argument name="checkoutSession" xsi:type="object">Magento\Checkout\Model\Session\Proxy</argument>
        </arguments>
    </type>

    <type name="Magento\Checkout\Model\CompositeConfigProvider">
        <arguments>
            <argument name="configProviders" xsi:type="array">
                <item name="pstk_paystack_config_provider" xsi:type="object">Pstk\Paystack\Model\Ui\ConfigProvider</item>
            </argument>
        </arguments>
    </type>
    <type name="Magento\Framework\App\Request\CsrfValidator">
        <plugin name="csrf_validator_skip" type="Pstk\Paystack\Plugin\CsrfValidatorSkip" />
    </type>

    <!-- CSP: Paystack domains are whitelisted via etc/csp_whitelist.xml (the Magento-standard
         mechanism). The former PHP PolicyCollector replaced Magento's entire CSP and stalled checkout. -->
</config>
