<?xml version="1.0" encoding="UTF-8"?>
<!--
  Admin "Stores > Configuration > Sales > Payment Methods" page.

  Declared as a proper area="admin" page object on purpose: MFTF normalizes
  admin page-object URLs to a leading-slash, base-relative form at generation
  time (e.g. "/admin/system_config/edit/section/payment"). A raw
  <amOnPage url="admin/system_config/..."/> string does NOT get that treatment
  and resolves relative to the current (post-login dashboard) URL, producing
  /admin/admin/system_config/... -> noRoute 404. That 404 is exactly what made
  PaystackPaymentConfigAvailableTest fail in the Adobe marketplace pipeline.
-->
<pages xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Page/etc/PageObject.xsd">
    <page name="AdminPaymentConfigPage"
          url="admin/system_config/edit/section/payment"
          area="admin"
          module="Pstk_Paystack">
    </page>
</pages>
