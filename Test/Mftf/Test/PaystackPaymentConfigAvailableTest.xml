<?xml version="1.0" encoding="UTF-8"?>
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="PaystackPaymentConfigAvailableTest">
        <annotations>
            <title value="Paystack payment method is available in admin configuration"/>
            <description value="Verify Paystack appears in Stores &gt; Configuration &gt; Sales &gt; Payment Methods when the extension is installed."/>
            <group value="Paystack"/>
        </annotations>
        <before>
            <!--
              Use Magento's standard admin login. The Adobe pipeline's own
              mftfmagento suite uses this action group and passes, so the prior
              concern about core action groups "failing to generate" does not hold.
            -->
            <actionGroup stepKey="loginAsAdmin" ref="AdminLoginActionGroup"/>
        </before>
        <!--
          Navigate via the area="admin" page object so MFTF emits a leading-slash,
          base-relative URL. A raw <amOnPage url="admin/..."/> resolves relative to
          the post-login dashboard and 404s (the original submission failure).
        -->
        <amOnPage stepKey="goToPaymentConfig" url="{{AdminPaymentConfigPage.url}}"/>
        <waitForPageLoad stepKey="waitForPaymentConfig" time="60"/>
        <see stepKey="seePaystackSection" userInput="Paystack"/>
        <after>
            <actionGroup stepKey="logout" ref="AdminLogoutActionGroup"/>
        </after>
    </test>
</tests>
